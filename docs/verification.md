# Проверки пакета 014

Дата: 08.09.2026. [Текущее ТЗ](TZ_TutorGate_014_schedule_chat_bot_accounts_performance.md).

Проверки используют закреплённые зависимости. Версии и lockfile не менялись. Цепочка миграций 001–014 выполняется в изолированном PGlite. E2E запускает настоящий Next с локальными Supabase, Storage и Telegram fixtures. Рабочая БД и реальный Telegram не изменялись.

## Команды

| Команда | Результат |
|---|---|
| `npm run lint` | Пройдено |
| `npm run typecheck` | Пройдено |
| `npm test` | 139/139, без пропусков |
| `npm run test:docs` | Пройдено: 23 Markdown-файла, 46 относительных ссылок |
| `npm run build` | Production-сборка успешна |
| `npm run test:e2e` | 78/79 за 6 минут; один timeout исправлен, целевой повтор 3/3 |

Логи: `artifacts/package-014-lint.log`, `artifacts/package-014-typecheck.log`, `artifacts/package-014-tests.log`, `artifacts/package-014-docs.log`, `artifacts/package-014-build.log`, `artifacts/package-014-final-e2e.log`. Четыре новых браузерных сценария отдельно пройдены: `artifacts/package-014-new-e2e.log`.

В ходе проверки исправлены устаревшие ожидания цвета completed-занятий, переключения недели при drag, числа кнопок меню бота и последние 200 сообщений. Storage fixtures дополнены обработкой бинарных загрузок. Общий прогон прошёл 78 сценариев; мобильный сценарий package-010 не дождался меню после синтетического contextmenu. Добавлено ожидание видимого меню с повтором действия, поскольку синтетический event не ждёт hydration. Команда `npm run test:e2e -- tests/e2e/package-010.spec.ts --grep 'empty menu uses selected mobile day' --repeat-each=3` прошла 3/3 за 15,7 с (`artifacts/package-014-mobile-recheck.log`). После изменения ожидания весь набор повторно не запускался.

Просмотрены актуальные снимки мобильного расписания `artifacts/return-button-390.png` и чата `test-results/package-011-011-failed-Tel-85998-ast-200-and-responsive-chat/chat-390.png`: элементы помещаются по ширине, форматирование и прикрепление файлов доступны. Каталог test-results перезаписывается следующим прогоном.

## Покрытие

- Расписание: приоритет effective color, сохранность base color, coral temporal lock в UI и DB, заметки без сдвига времени, ограничения владельца/delegated admin, недельная загрузка и отсутствие edge navigation. Старые suites продолжают проверять magnet, exclusion constraints и undo/redo.
- Чат: безопасные structured marks и ссылки, HTML escaping и разбиение Telegram-сообщений, UTF-16 entities, rich paste без исполнения HTML, файлы через input/drop, удаление из draft, несколько вложений, входящая фотография, граница 10 МБ и DB participant/grants. Delta включает delivery transitions; mapping сохраняется для каждой доставленной части при частичном сбое.
- Аккаунты: запрет удаления admin/чужой роли, очистка зависимостей, сохранность других пользователей, запрет раннего purge до истечения upload URL, повторение после сбоя между DB и Auth.
- Бот: меню по ролям, проверка admin identity, очередь заявок через общий DB-домен. Общие application suites проверяют существующие атомарные review/token правила.
- UI: центрирование Select и ограничения viewport, desktop/mobile support link, логотип без точки.

## Производительность

Изолированный PGlite: 10 000 занятий, 10 000 сообщений, 5 053 профиля и 5 001 назначение; 20 прогретых измерений. Скрипт: `node scripts/benchmark-014.mjs`. Время ниже — время локального запроса, без сети и замера накладных расходов RLS; это не production SLA.

| Операция | p50 до → после, мс | p95 до → после, мс | JSON до → после, байт |
|---|---:|---:|---:|
| Начальное расписание → неделя | 206,73 → 3,03 | 262,97 → 4,89 | 6 030 001 → 4 640 |
| Poll чата без изменений | 124,54 → 6,97 | 169,24 → 9,75 | 62 455 → 115 |
| Сохранение расписания | 1 827,81 → 1 473,91 | 2 274,97 → 1 525,21 | 5 385 654 → 5 385 654 |
| Каталог → страница 50 | 65,12 → 4,98 | 87,14 → 5,46 | 1 048 218 → 32 240 |

Baseline расписания читает полные raw rows, новая неделя включает имена и варианты формы. Каталог сравнивает полный список с серверной страницей и лёгкими вариантами преподавателей. Mutation сохраняет полный канонический owner snapshot; его объём и стоимость остаются ограничением на длинной истории. Замеры и EXPLAIN ANALYZE: `artifacts/package-014-performance.json`. Недельный запрос использует существующий `lessons_tutor_time`, delta — новый индекс conversation/revision. Для статистики достаточно существующего индекса.

## Выпуск и ограничения staging

Миграцию `supabase/migrations/202609070014_product_polish_chat_files_hard_delete_performance.sql` применять целиком после 013 перед запуском новой версии. Исторические миграции не изменены. Миграция создаёт private bucket и отзывает старые подписанные schedule history.

В доверенном серверном scheduler необходимо настроить ежечасный запуск `node scripts/cleanup-chat-storage.mjs` с серверными переменными Supabase. Он удаляет брошенные загрузки после защитного срока; ошибки допускают безопасный повтор. В текущем окружении scheduler не настроен.

Удаление аккаунта блокирует доступ сразу. Если могли остаться действующие signed upload URL, финальное удаление ждёт защитного срока (до 2 часов 5 минут) и завершается кнопкой повтора в админке. Успех показывается только после Storage, DB и Auth.

До production остаётся реальный staging smoke test из раздела 19 ТЗ: форматирование/фото/документ/Reply/превышение 10 МБ в обе стороны, одновременные действия двух admin над заявкой, удаление тестового пользователя с Storage/Auth и повторная заявка того же Telegram, delegated/coral и mobile. Нужны также сетевые performance-замеры staging. Fixtures не подтверждают реальную внешнюю доставку, signed URL Supabase или состояние deployed Storage. Полный staging Definition of Done пока не подтверждён.

## Доработка чата, поддержки и панели расписания — 08.09.2026

Компактный composer с иконками, повторным снятием форматирования и автоподстройкой высоты. Поиск диалогов, карточки файлов, автоматические приватные превью, крупный просмотр и скачивание с повторной проверкой доступа. Поддержка вынесена в единственную круглую кнопку справа внизу; «Обозначения» расположены рядом с биндами.

Проверки: 139/139 unit/DB; 13/13 целевых E2E (`chat-polish`, `package-011`, `package-014`). В промежуточном прогоне один login превысил 5 секунд на фоне параллельного DB suite; повторный прогон прошёл 13/13. Новые E2E проверяют toggle форматирования, черновики между диалогами, превью и диалог изображения, скачивание, поиск, положение поддержки и отсутствие перекрытия кнопки отправки на 320/390/768/1440 px. Логи — `artifacts/chat-polish-*.log`; снимки `artifacts/chat-polish-1440.png`, `artifacts/chat-polish-320.png` и `artifacts/chat-polish-with-image.png` просмотрены. Внешняя доставка остаётся проверенной только на fixtures. Новые миграции для этой доработки не нужны.

После доработки также пройдены lint без предупреждений, TypeScript, production build и проверка Markdown-ссылок. В документации исправлены прежние ссылки на отсутствующие отчёты 010–012 и отсутствующий архив; актуальные ссылки ведут к ТЗ 014 и verification.

## Пакет 015 — финальная проверка, 08.09.2026

Реализованы live rich editor без отдельного пункта ссылки, автоссылки сообщений, общий файловый бюджет 10 МБ, очистка чата после снятия последнего назначения, широкое окно без прокрутки страницы, иконка палитры и новый сценарий бота. Внешняя БД не изменялась: перед запуском требуется применить миграцию 015 после 014 и использовать обновлённый скрипт фоновой очистки Storage.

| Проверка | Результат |
|---|---|
| `npm test` | 144/144, включая полную цепочку миграций и новые DB-регрессии |
| `npm run lint` | Без ошибок и предупреждений |
| `npm run typecheck` | Пройдено |
| `npm run build` | Пройдено |
| `npm run test:docs` | Пройдено |
| `npm run test:e2e` | 81/82 за 7,1 минуты до исправления объединения styled runs |
| Повтор chat-polish, package-011, package-014, bot-control | 16/16 после исправлений |

Один новый тест полного прогона выявил отдельный styled run на каждый символ при выборе стиля до набора. Соседние одинаковые runs теперь объединяются, что также сокращает AST и Telegram HTML. При первом повторе уточнён селектор ошибки общего лимита: `role=alert` находил также служебный announcer Next. Итоговый повтор прошёл 16/16; полный набор после этих исправлений повторно не запускался.

Новые проверки покрывают форматирование непосредственно в поле и при наборе, Shift+Enter, отсутствие пункта ссылки, активные ссылки в сообщениях, файлы 6+5 МБ и ровно 10 МБ в сумме, запрет обхода бюджета service-записью, проверенную Telegram mapping, удаление пары/вложений и очередь физической очистки. E2E бота проверяет меню, выбор даже одного преподавателя, отмену к выбору, квитанцию, удаление входного сообщения и prompt, обновление исходного сообщения преподавателя после ответа. Desktop/mobile 1440/768/390/320 проверены на отсутствие переполнения по обеим осям; актуальные снимки 1440/320 просмотрены.

Логи: `artifacts/chat-015-tests.log`, `artifacts/chat-015-lint.log`, `artifacts/chat-015-typecheck.log`, `artifacts/chat-015-build.log`, `artifacts/chat-015-docs.log`, `artifacts/chat-015-full-e2e.log`, `artifacts/chat-015-recheck.log`. Telegram и Storage в браузерных тестах — локальные fixtures; реальные внешние удаления и доставка на staging в этой проверке не выполнялись.


## Проверка уточнений 016 — 08.09.2026

Внесены восемь запрошенных уточнений: удаление двух подписей чата, тонкий тематический скролл вложений, tooltip «Обозначения», Telegram-контакты без username через проверяемую карточку бота, поддержка в `/start`, звук входящих, выключение всех шести стилей и поиск/компактные карточки заявок.

- `npm test` — 147/147, включая миграцию 016, поиск до пагинации и запрет доступа к контактам чужим ролям.
- `npm run test:e2e -- tests/e2e/chat-polish.spec.ts tests/e2e/bot-control.spec.ts` — 7/7. После добавления проверки нового deep link повторный `bot-control.spec.ts` — 3/3. Это 8 разных браузерных сценариев; полный E2E-набор не запускался. Проверка звука наблюдает создание AudioContext oscillator при реальном обновлении fixture-чата.
- `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test:docs`, `git diff --check` — выполнены успешно.
- Артефакты: `artifacts/chat-016-tests.log`, `chat-016-e2e.log`, `chat-016-bot-final.log`, `chat-016-lint.log`, `chat-016-build.log`; снимки `chat-016-scroll.png`, `applications-016-1440.png`, `applications-016-390.png`, `applications-016-320.png`. Десктопные снимки просмотрены.

Первый прогон выявил устаревшее ожидание двух кнопок `/start`; оно обновлено для поддержки. Проверка звука выявила отсутствие уведомления при полном snapshot без delta cursor; обработка исправлена, повторная проверка прошла.

Перед развёртыванием применить [миграцию 016](../supabase/migrations/202609080016_application_search.sql) после 015. Внешняя БД и реальный Telegram не использовались. Браузерный звук работает после взаимодействия с сайтом и при действующем polling активной вкладки; доступность Telegram-профиля в реальном клиенте зависит от Telegram.


## Проверка исправлений 017 — 08.09.2026

- `npm test` — 148/148. DB-регрессия воспроизводит ошибку прежнего invoker-триггера при service-role обновлении username и проверяет исправление. Альбом сохраняет второй файл после сброса recipient в тот же message ID; повтор update не дублирует файл, новая группа не наследует адресата, лимит суммы и закрытые grants проверены.
- `npm run test:e2e -- tests/e2e/package-017.spec.ts tests/e2e/package-010.spec.ts --grep "017|Telegram sync"` — 3/3. Настройки администратора, альбом из 3 фото и альбом из 3 документов; у всех файлов проверены превью после прокрутки, удаление входных Telegram сообщений и отсутствие дублей. Превью вне области просмотра загружаются лениво.
- Typecheck, lint, production build и diff-check — успешно. Полный E2E-набор не запускался. Логи: `artifacts/chat-017-tests.log`, `chat-017-db.log`, `chat-017-e2e.log`, `chat-017-lint.log`, `chat-017-build.log`.

Для запуска применить [миграцию 017](../supabase/migrations/202609080017_telegram_sync_albums.sql). Внешняя БД и реальные аккаунты Telegram не изменялись; проверки используют локальную БД и API fixtures.

## Пакет 018 — 09.09.2026

Реализовано [ТЗ 018](TZ_TutorGate_018_chat_schedule_rates_background.md): rich content v2 с чтением v1, LaTeX и live-preview, inline/block code с копированием и Telegram HTML, toolbar со списками/выравниванием/историей, административные ставки tutor и tutor + student, snapshot ставки проведённого занятия, Telegram animation/WebP/TGS/WebM, ручной МСК-сдвиг и персональный private background до 7 МБ. Сохранены delegated ограничения, server validation, RLS и подписанный undo/redo расписания.

- `npm test` — 159/159. DB suite применяет цепочку 001–018, проверяет backfill прежних completed lessons, три уровня ставок, неизменяемость snapshot и его подписанное восстановление, grants, self/delegated background, очередь очистки, rich v1/v2 и Telegram dedupe.
- `npm run lint`, `npm run typecheck` — успешно, без ошибок.
- Снимки `artifacts/018-chat-mobile.png` и `artifacts/018-video-background.png` просмотрены: мобильный composer без переполнения, компактное поле МСК, читаемая сетка поверх видео. Браузерный тест проверяет pause при reduced motion и сохранение прежнего фона после отклонения файла больше 7 МБ.

Регрессии выявили и помогли исправить ширину поля МСК на мобильном экране, ранний ввод до hydration редактора, Enter в пустом пункте списка, снятие вложенного code block и ранний клик по ещё неактивной кнопке копирования. Проверка копирования нормализует только системные CRLF/LF, сохраняя пробелы и табы.

Перед запуском применить [миграцию 018](../supabase/migrations/202609080018_chat_schedule_rates_background.sql) после 017. Внешняя БД, реальный Storage и Telegram не изменялись: браузерные тесты используют только локальные fixtures. Ранее проведённые занятия при миграции получат общую ставку на момент её применения; исторически точные ставки до 018 восстановить без прежнего журнала ставок нельзя. Очистка старых фонов и заброшенных uploads включена в существующую фоновую очистку, с отсрочкой до истечения signed upload URL.

## Уточнения 018 — итоговая проверка 09.09.2026

Выполнены [восемь последующих уточнений](TZ_TutorGate_018_followup.md). Для формул/выравнивания добавлена отправка `sendRichMessage` со структурированными native blocks. Протокол сверен с актуальной документацией Telegram; тесты проверяют LaTeX entities, align center/right, literal code, malformed fallback и сохранение reply callback. Простые сообщения сохраняют прежний способ доставки.

| Проверка | Фактический результат |
|---|---|
| `npm test` | 162/162, включая DB-цепочку и новые Telegram rich-регрессии |
| `npm run lint` | Пройдено без ошибок и предупреждений |
| `npm run typecheck` | Пройдено |
| `npm run build` | Production-сборка прошла |
| `npm run test:docs` | Пройдено |
| E2E: package-018, package-018-editor-background, package-018-followup, app, package-011, chat-polish, bot-control | 30/30 за 2,7 минуты |
| `git diff --check` | Пройдено |

До последующих уточнений полный E2E-прогон завершился 92/93: один вход на административную страницу превысил ожидание 5 секунд. Этот сценарий повторно прошёл в итоговых 30/30. Полный набор после последующих уточнений не повторялся. При разработке новых UI-тестов исправлено обращение к скрытому мобильным днём занятию и ожидание hydration перед синтетической вставкой; тест преобразования кода обнаружил реальную потерю BR-переноса, чтение исправлено и проверяется точное содержимое блока.

Снимки модальных окон ставок и фона сделаны на 1440/375/320 px; проверены размеры без горизонтального переполнения и доступность закрытия. Просмотрены `artifacts/018-tutor-rate-375.png`, `018-pair-rate-320.png`, `018-background-filled-375.png`, `018-section-background.png`, `018-code-highlight.png`. Фон находится за всем разделом, код подсвечен без указания языка, у всех трёх типов sticker отсутствует файловая строка. МСК отклоняет выход за ±12 непосредственно при вводе и вставке.

Логи: `artifacts/test-018-complete.log`, `lint-018-complete.log`, `typecheck-018-complete.log`, `build-018-complete.log`, `docs-018-complete.log`, `e2e-018-regression-final.log`. Проверки Telegram/Storage используют локальные fixtures, реальная отправка ученикам не выполнялась. Для этих уточнений дополнительная миграция не нужна; внешние аккаунты и БД агентом не изменялись.

## Пакет 019 — 09.09.2026

Выполнены семь новых уточнений: исправлены поле МСК и обрезка нижних пределов формул, три действия отключены при мультивыделении, зелёный/голубой убраны из палитры, toolbar следует за выделением и имеет одно активное выравнивание, GIF без файловой строки, MP4 с предпросмотром, административный просмотр чатов репетитора через иконку перед шестерёнкой.

- `npm test` — 163/163. После усиления проверки пагинации повторный `tests/package-019-database.test.ts` — 1/1: сообщения с одинаковым временем не теряются, роли/owner и вложения проверяются в реальной локальной SQL-базе, identity и read markers не меняются, снятие назначения закрывает доступ.
- `npm run lint`, `npm run typecheck` — успешно.
- E2E `package-019`, `chat-polish`, `package-018-editor-background` — 11/11. Форматирование проверено на выделенном слове с четырьмя стилями, обычном тексте, переключении right → center и undo. Проверены формулы при 320/375 px, мультивыделение и фактическая загрузка MP4 в HTML video.
- Просмотрены снимки `artifacts/019-formula-320.png`, `019-offset.png`, `019-menu.png`, `019-admin-chat.png`, `019-admin-chat-mobile.png`. Поле МСК без зависшей ошибки, формула с нижним пределом целиком, меню показывает три цвета и отключённые действия; административная история помещается на мобильном экране.

Для административного просмотра требуется применить [миграцию 019](../supabase/migrations/202609090019_admin_chat_read_access.sql) после 018. Внешняя БД не изменялась; E2E использует локальные fixtures, реальная доставка Telegram и deployed Storage этим прогоном не подтверждаются.

Дополнительный E2E-прогон `package-019`, `schedule`, `schedule-features`, `package-011`, `package-018` — 33/33 за 3,1 минуты. Проверены прежние переносы, undo/redo, мобильный drag, назначение/удаление диалогов, просмотр истории и загрузка MP4 администратором. Полный E2E-набор в этом пакете не запускался.

`npm run build` — успешно; `npm run test:docs` — успешно (13 Markdown-файлов, 39 относительных ссылок). Логи: `artifacts/tests-019-all.log`, `db-019.log`, `lint-019.log`, `typecheck-019.log`, `e2e-019.log`, `e2e-019-regression.log`, `build-019.log`, `docs-019.log`.

## Пакет 020 — 09.09.2026

Исправлены отступ поиска и длинные имена в административном чате. Фон расписания занимает всю dashboard-content, включая поля и заголовок. Добавлены три медленно движущиеся декоративные окружности на сайте; reduced motion отключает анимацию. Настройки администратора сохраняют пакеты, преамбулу и собственные .sty/.asy. Конфигурационный файл Asymptote исключён по уточнению пользователя. Подготовлен отдельный контейнерный рендерер для полноценных TeX/TikZ/Asymptote и кнопка получения PNG в веб-чате.

- `npm test` — 166/166; после добавления отдельного `tests/latex-service.test.ts` он запущен отдельно, 1/1. DB suite применяет цепочку 001–020, проверяет роли, блокировку аккаунта, закрытую таблицу, типы/размеры/пути файлов. Тест HTTP-брокера проверяет отсутствие авторизации, превышение размера и недоступный Docker.
- E2E `package-020`, `package-019`, `package-018-editor-background` — 10/10. Дополнительная проверка точного списка пакетов выявила ранний ввод до hydration; ввод блокируется до готовности формы. Повторный `package-020` после исправления — 4/4. Полный E2E-набор не запускался.
- Снимки `artifacts/020-admin-chat-1440.png`, `020-background-1440.png`, `020-settings.png` просмотрены. Проверены также ширины 375/320, отсутствие горизонтального переполнения и совпадение границ фонового слоя с разделом.
- `npm run typecheck`, `npm run lint` — успешно. Проверен синтаксис Node-брокера, smoke-скрипта и Python-рендерера.

Для запуска нужны [миграция 020](../supabase/migrations/202609090020_latex_settings.sql) и [сервис компиляции](latex-renderer.md). Docker/TeX отсутствуют в текущем Windows-окружении: образ и настоящая компиляция не запускались; E2E получает PNG от локального fixture-сервиса. Фактическую работу шрифтов/пакетов/TikZ/Asymptote должен подтвердить `services/latex/smoke.mjs` после запуска образа. Внешние БД, Telegram и инфраструктура не менялись. Новая компиляция применяется к веб-чату, Telegram-доставка в этом пакете не расширялась.

Логи: `artifacts/tests-020-all.log`, `tests-020-target.log`, `latex-broker-test.log`, `e2e-020.log`, `e2e-020-final.log`, `lint-020-final.log`, `typecheck-020.log`.

Финальная проверка 10.09.2026: `npm run build` завершил production-сборку; `npm run test:docs` — 14 файлов и 44 относительные ссылки без ошибок; `git diff --check` — без ошибок. Логи сборки и ссылок: `artifacts/build-020.log`, `artifacts/docs-020.log`.

## Пакет 021 — 10.09.2026

Убраны Docker/HTTP-компилятор и поле «Преамбула и команды». Реальное отображение выполняется в браузере: KaTeX, локальный TikZJax WASM и ограниченный 2D Asymptote. Пользовательский код не отправляется внешним сервисам. Добавлены SVG-контуры фона, частицы, лёгкая реакция на указатель и остановка при reduced motion/скрытой вкладке. Установка системного TeX не требуется; поддержка произвольных пакетов и полного языка Asymptote не заявляется. [Возможности и ограничения](latex-renderer.md).

- `npm test` — 169/169, включая реальные SQL-миграции и новые регрессии 2D-грамматики, ошибок/числовых ограничений, экранирования подписей и библиотек команд. Удалён тест удалённого Docker-брокера.
- E2E `package-019`, `package-020`, `package-021`: первоначально 10/11; проверка библиотеки выявила конфликт стандартного пользовательского имени R с предопределённым макросом KaTeX. Определения адаптированы, повторный `package-020` + `package-021` — 7/7. Четыре сценария 019 прошли в первом прогоне. Полный E2E-набор не запускался.
- В браузерных тестах рендерер настоящий: получены SVG путей TikZ и Asymptote, а fixture HTTP-компилятора удалён. Бесконечный TikZ-цикл принудительно завершается через 20 секунд, пока элементы страницы остаются доступными. Проверены сохранение и применение .sty-команды, отсутствие поля преамбулы, реакция фона на указатель и reduced motion.
- `npm run typecheck`, `npm run lint` — успешно. Снимки `artifacts/021-tikz.png`, `021-asy.png`, `021-login.png` просмотрены: реальные линии/окружности/подписи и ненавязчивый фон, без свечения и градиентов.

Новой миграции нет. Для сохранения библиотек используется прежняя 020, legacy preamble обнуляется Server Action. Версии npm и lockfile не менялись; статические файлы браузерного движка и шрифты добавлены с лицензиями, описанием изменений и контрольными суммами. Внешняя БД и Telegram не изменялись.

Логи: `artifacts/tests-021-all.log`, `tests-021-target.log`, `e2e-021-final.log`, `e2e-021-recheck.log`, `typecheck-021.log`, `lint-021-final.log`, `build-021.log`.

`npm run build` — успешно, `npm run test:docs` — успешно; production-сборка включает локальные ресурсы worker/WASM. `git diff --check` — без ошибок.
